#ifndef SCREEN_LOCK_SIMULATOR_H
#define SCREEN_LOCK_SIMULATOR_H

void simulate_screen_lock(int current_time);
int is_screen_locked();  // Returns 1 if screen is locked, 0 if unlocked

#endif // SCREEN_LOCK_SIMULATOR_H